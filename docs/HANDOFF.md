# Handoff

Current, durable state of the project. Read this first when resuming.

## Where things are

- **Live headline numbers**: [CURRENT_STATE.md](CURRENT_STATE.md) — generated by every build,
  drift-guarded by `test_current_state_doc_is_in_sync`. Never hand-quote a number that lives
  there; point at it.
- **Latest iteration**: [ITERATION_9_REVIEW.md](ITERATION_9_REVIEW.md). Prior passes:
  ITERATION_1..8_REVIEW.md, MVP_REVIEW.md.
- **Pipeline network**: [PIPELINE_GAP_AUDIT.md](PIPELINE_GAP_AUDIT.md) (why it was broken),
  [PIPELINE_SOURCE_AUDIT.md](PIPELINE_SOURCE_AUDIT.md) (source matrix + licences),
  [POWER_SOURCE_RECONCILIATION.md](POWER_SOURCE_RECONCILIATION.md) (an open denominator defect).
- **How to read a number**: [METHODOLOGY.md](METHODOLOGY.md). Zeros: [ZERO_COUNT_AUDIT.md](ZERO_COUNT_AUDIT.md).
- **Build/run**: `.venv\Scripts\python.exe -m pipeline.run` (pipeline + web payload);
  `cd web && ..\scripts\npm.cmd run dev -- --port 5178 --strictPort` (frontend). Tests:
  `.venv\Scripts\python.exe -m pytest tests/test_pipeline.py`; `cd web && ..\scripts\npm.cmd run typecheck`;
  `..\scripts\npm.cmd run test` (vitest). Deterministic per `--as-of`; use `--as-of 2026-08-28` as
  the frozen reference when decomposing a change so time decay doesn't contaminate attribution.

## Iteration 9 end state (theme: pipeline network from real sources — data/GIS, not scoring)

Merged from `iteration-9`. See [ITERATION_9_REVIEW.md](ITERATION_9_REVIEW.md).

**The context network is now reconstructed from OSM route RELATIONS, not loose ways.** The old
builder applied a 50 km minimum to each OSM *way*, required a name on each way, queried bands that
left 56–120°E unasked, and deleted anything the analytic feed also had. That cost 161,899 km of
relation-member geometry and hid the entire Russian trunk backbone whenever the analytic layer was
off. Gas went 220 routes / 29,832 km -> 375 routes / 164,563 km; oil 75 / 12,630 -> 89 / 51,385.

Rules that are now structural, and are tested:

- **Reconstruct the route, then judge it.** The trunk threshold applies to the ASSEMBLED route.
  84 routes exist only because assembly precedes the filter.
- **Proximity is not connection.** Segments join only on exactly-shared endpoints. The single
  distance rule (`weld`, <=100 m) applies ONLY between components of the same relation, and is
  counted per route. The named-way path never welds.
- **The context layer is self-contained.** Overlap with the analytic feed is MARKED
  (`analytic_overlap`), never deleted; the frontend suppresses double-draw.
- **Topology known != geometry known.** 54 sourced connection assertions live in
  `data/curated/pipeline_topology.csv`; two tests stop them ever becoming a drawn line.
- **Route quality is MEASURED** from source vertex density, never asserted.

**Traps a future session will hit:**

- `geo._perpendicular_distance` measures to the INFINITE LINE. That is fine for well-separated
  anchors and wrong for chains that return near their start — it erased 690 components / 216 km
  before it was caught. `build_pipeline_network` has its own point-to-SEGMENT version;
  `build_assets`, `build_context` and `geo.simplify_ring` **still use the broken one**.
- OSM models some systems as a superroute PLUS its child relations, so summing route lengths
  double-counts ~13%. Use `distinct_network_km`, not `total_length_km`, for network extent.
- OSM `substance=oil` covers refined products too. Exolum tags 235 members `oil` and zero `fuel`;
  only the NAME rule excludes it.
- ENTSOG has **no pipeline entity** — operator/point/balancing-zone only — and its `tpMapX/Y` are
  schematic, not lat/lon.
- GEM wiki **status** fields are stale (it says Ukraine transit is operating; it stopped
  2025-01-01). Where a tracker and a TSO disagree on status, the TSO wins.

## Iteration 8 end state (theme: symbology, trend views & analyst UX — features, not scoring)

Merged from `iteration-8`. A visualisation/navigation pass only; no scoring, weights, or corpus
touched. See [ITERATION_8_REVIEW.md](ITERATION_8_REVIEW.md).

**Read this first — the two builds.** Production and the frozen reference are different on
purpose and both are correct:

| Build | Command | as_of | ESDI |
|---|---|---|---|
| production / release | `python -m pipeline.run` | build day | moves with time decay |
| frozen regression | `python -m pipeline.run --as-of 2026-08-28` | 2026-08-28 | **18.49**, stable across iterations |

The frozen build is for apples-to-apples methodology comparison ONLY and must never be committed
as the release payload — it would freeze the live dashboard at a stale date.
`test_release_payload_is_a_current_date_build_not_a_frozen_reference` now fails the suite if one
is, so run a plain `python -m pipeline.run` before committing after any comparison run.

**A `dataset-refresh[bot]` rebuilds and commits the data daily** (`.github/workflows/refresh.yml`:
current-date `python -m pipeline.run`, then `pytest`). Consequences worth knowing:

- Expect committed data files to change on `origin/main` without a human touching them, and
  expect a merge conflict in `data/processed/` + `web/public/data/` on any branch that also
  rebuilt. Resolve it by **regenerating** (`python -m pipeline.run`), never by picking a side —
  the database is a build artifact.
- The bot's build inherits whatever pipeline code is on main, so a pipeline change ships into the
  daily payload automatically from the next run.
- Any new test must pass against a bot-produced current-date payload, because the workflow runs
  the suite immediately after rebuilding.

- **Infrastructure icons** replace the old asset-dots. `src/icons.ts` is the single registry
  driving map + filter rows + legend (shape = function, colour = class identity, dashed frame =
  region-centroid placement). Icons rasterise **locally** to `addImage` — the zero-network
  invariant is preserved (no glyph service). Declutter via `symbol-sort-key`, never a target rank.
- **Trend surfaces (all opt-in; default view = exposure ESDI, cumulative halos, unchanged):**
  diverging "Change in ESDI" 30/90-day choropleth (`ESDI_DELTA_STOPS`, blue fell / red rose),
  recent-activity halos (30/90/cumulative — "activity", never impairment), and a "What changed"
  dossier tab (7/30/90d) keeping new-events / new-restoration / ESDI-delta **separate**.
- **Navigation:** full **deep-link state** in the URL (`urlState.ts` — surface, window, timeline
  date, region, filters, layers, compare set, camera; only non-defaults written), ESDI-trajectory
  **sparklines**, a 3-region **comparison tray**, and region/facility **search** (public names only).
- Two defects fixed in passing: asset-symbols `minzoom` 3.4→2 (icons now show at the home view);
  `rivers` `line-opacity` reformulated (invalid nested `["zoom"]` had silently dropped the layer
  since iteration 5 — proven against MapLibre's own style validator).

**Release-gate findings (read before touching recovery or trend views):**

- **`live_disruptions` is NOT the recovery record.** It holds only facilities whose disruption
  weight is still > 0 and is capped at 80, so fully-recovered facilities are absent by
  construction. Using it to answer "what recovery landed recently?" undercounted 9 episodes to 1.
  The complete log is **`snapshot.recovery_events`** (emitted by `_recovery_events()`), one row per
  (episode, evidence date), with `counts_toward_observed_episodes` marking the subset that carries
  a measured duration. Ask which question you mean before choosing a source.
- **"30/90-day change" is really 28–35 / 84–91 days** — the index series is weekly. Always resolve
  a window through `data.windowRef()`, which reports `actualComparisonDays` and `comparisonDate`
  and can never resolve past the scrubber. All four change surfaces share it.
- **14 of 35 region-precision assets share a centroid with another.** The map draws one stacked
  marker per centroid and names every member; never jitter them apart — inventing offsets would
  fabricate geography the dataset does not have.

## Iteration 7 end state (theme: analytic integrity, not features)

Committed on `iteration-7`. Headline unchanged (real-date ESDI 18.17 / frozen 18.49) — the pass
made numbers more defensible, not larger. Highlights:

- **Refining denominator (§3-7)**: no major crude refinery is missing; the ~53 MTPA gap to the 327
  nameplate reference = ~24 MTPA condensate splitters (excluded like Tobolsk) + ~29 MTPA
  conservative-basis understatement. Kept the one consistent source basis (no revalue → no ESDI
  change); emitted completeness metadata + the plain statement that refining struck-shares are
  conservative UPPER bounds.
- **Recovery state model (§9-15)**: three orthogonal concepts — damage severity (concept A, from
  `incident.status`, applied ALWAYS) vs recovery (concepts B/C, the record sets decay/cap). Adding a
  recovery record is now structurally monotonic; the iteration-6 status-coupling bug is impossible.
  `'repaired'` is no longer a damage state (migrated). Evidence families (facility_reconstitution /
  unit_restart / service_restoration / flow_rerouting / estimate). Corpus 20→25 records, 7→9 observed.
- **Gas processing (§16-20)**: STAYS EXPERIMENTAL (no matched external denominator; census mixes
  design+actual). Minnibaevo corrected 0.45→0.8. Never summed with LNG/pipelines into a "Gas" score.
- **Transmission (§22-26)**: independent red-team → KEEP Model A, relabel "**Transmission burden**",
  publish the ex-transmission counterfactual (18.10). Alternatives B/C/D/E all computed. Crimea
  untouched (~45% of the burden by analytic choice).
- **§27**: `esdi_all_sectors` → `uncovered_zero_assumption_sensitivity` (a sensitivity under a false
  assumption, not a 2nd ESDI; old field kept as N-1 alias).
- **Effects (§28-31)**: 15→26; added `source_quality` tier. **Semantic lint (§1)**: a test now fails
  the build if any source comment/doc wrongly fences occupied Crimea out of the aggregate (the
  stale-phrasing bug that shipped in iters 5-6).

## Non-obvious things to know

- **`esdi_excluded` is EMPTY.** Occupied Crimea is `esdi_included=True` and DOES feed the headline
  (~45% of transmission, +1.10 ESDI). Never write a comment/doc saying occupied regions are fenced
  out — a lint test now fails the build if you do. (This bug shipped in iters 5 and 6.)
- **Transmission is "Transmission burden"**, an event-burden proxy against an arbitrary saturation
  of 8 — never "% of grid offline". ~45% is Crimea. The formula is retained deliberately; a future
  pass could revisit it.
- **The refining base is a conservative single-source vintage** ~10-15% below nameplate. This does
  NOT bias the refining *exposure* (a capacity share is invariant under uniform revaluation); it
  only understates the absolute MTPA + the completeness ratio. A uniform nameplate revaluation is a
  candidate future change.
- **Never fix data by editing `data/processed/`** — it's a build artifact. Fix the curated source
  and rebuild. All Cyrillic Python I/O must pass `encoding="utf-8"`.
- **Infrastructure symbology has ONE source of truth: `web/src/icons.ts`.** Add or change a class
  glyph there and it updates the map, the filter rows, and the legend together. Never hardcode a
  shape in a component. The map registers icons via local rasterisation only — do not reach for a
  glyph endpoint or sprite CDN (it would break the zero-network invariant).
- **The committed data is pinned to the FROZEN reference (`--as-of 2026-08-28`, ESDI 18.49)** as of
  iteration 8, not a real-date build. `--as-of` still defaults to `date.today()`, so a plain
  `python -m pipeline.run` will regenerate real-date and change the headline — rebuild with
  `--as-of 2026-08-28` to keep the frozen reference, or deliberately choose real-date.
- **MapLibre only allows `["zoom"]` as the direct input to a top-level interpolate/step.** Nesting
  it inside a `case`/comparison makes `addLayer` reject the layer silently (it fires an error event,
  doesn't throw). If a map layer "disappears", check its paint expressions for a nested `["zoom"]`.
- **`window.__map` exists only in dev** (`import.meta.env.DEV`) for map debugging; it is stripped
  from production builds.
- **Research episodes must be re-sourced from public news with real URLs**, never reconstructed from
  memory (agent findings are lost to compaction).

## Natural next steps (not started)

- Uniform full-nameplate revaluation of the refining base (removes the conservative bias) — needs
  comprehensive per-plant sourcing to avoid a mixed basis.
- Grow transmission/oil-terminal recovery toward any PHYSICAL-reconstitution duration (currently
  only service re-energisation exists for those classes).
- Revisit the transmission saturation constant / a physically-grounded transmission measure.
- Consider a reachable no-key candidate-discovery feed (GDELT is unreachable here; discovery is manual).
